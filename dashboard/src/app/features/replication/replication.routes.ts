import { Routes } from '@angular/router';
import { ReplicationComponent } from './replication.component';

export const replicationRoutes: Routes = [
  { path: '', component: ReplicationComponent },
];
