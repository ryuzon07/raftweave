import { Routes } from '@angular/router';
import { BuildPipelineComponent } from './build-pipeline.component';

export const buildPipelineRoutes: Routes = [
  { path: '', component: BuildPipelineComponent },
];
